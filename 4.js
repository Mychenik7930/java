const userAnswer = prompt('КАК ВАС ЗОВУТ');
const username = userAnswer;
username.toUpperCase(username);
const AgeAnswer = prompt('сколько вам лит');
let age = Number(AgeAnswer);
alerting = (`првиет ${username.toUpperCase(username).replace(/\s+/g, '')} тебе ${age}`);
console.log(alert(alerting,))